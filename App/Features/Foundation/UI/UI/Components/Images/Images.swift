import UIKit

public enum Images {
  public enum Buttons {
    static let addPlus = UIImage(name: "icon.button.add.plus")
  }
  
  public enum Checkbox {
    static let checked = UIImage(name: "icon.checkbox.checked")
    static let unchecked = UIImage(name: "icon.checkbox.unchecked")
  }
  
  public enum Navigation {
    public static let back = UIImage(name: "icon.navigation.back")
    public static let close = UIImage(name: "icon.navigation.close")
  }
  
  public enum Test {
    public static let bestGame = UIImage(name: "need_for_speed_most_wanted")!
  }
}
