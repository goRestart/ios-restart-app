<%
@terms.each do |term| 
if term.restriction == 'i' || term.restriction == nil
  if term.is_comment?
%>// <%= term.values[@current_lang] %>
<%  else 
	  if term.has_comment?
%>// <%= term.comment %>
<%
	  end 
%>"<%= term.keyword_iphone %>" = "<%= term.values_iphone[@current_lang] %>";

<%
    end
  end
end %>